{
  "name": "fichaya",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app",
    "start-angular": "cd angular-src && ng serve",
    "build": "cd angular-src && ng build",
    "deploy": "git add . && git commit -m 'deploy' && git push heroku master && git push origin master && git push github master",
    "push": "git push heroku master && git push origin master && git push github master",
    "deploy-prod": "cd angular-src && ng build --env=prod && cd .. && git add . && git commit -m 'prod' && git push heroku master && git push github master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://tobie_eniafe@bitbucket.org/tobie_eniafe/fichaya-web.git"
  },
  "author": "tobieeniafe",
  "license": "ISC",
  "homepage": "https://bitbucket.org/tobie_eniafe/fichaya-web#readme",
  "dependencies": {
    "express": "^4.16.3"
  }
}
